diff -up llvm-project-release-13.x/libcxx/include/__config.21~ llvm-project-release-13.x/libcxx/include/__config
--- llvm-project-release-13.x/libcxx/include/__config.21~	2021-08-02 22:28:58.851830387 +0200
+++ llvm-project-release-13.x/libcxx/include/__config	2021-08-02 22:30:03.342154641 +0200
@@ -10,6 +10,13 @@
 #ifndef _LIBCPP_CONFIG
 #define _LIBCPP_CONFIG
 
+#include <features.h>
+#if defined(__linux__) && !defined(__ANDROID__) && !defined(__GLIBC__)
+// If it's Linux, but neither GLIBC nor Android, it's likely musl...
+// (uClibc defined __GLIBC__ for compatibility)
+#define _LIBCPP_HAS_MUSL_LIBC
+#endif
+
 #include <__config_site>
 
 #if defined(_MSC_VER) && !defined(__clang__)
