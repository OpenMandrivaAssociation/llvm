diff -up llvm-project-release-15.x/libcxx/include/__config.19~ llvm-project-release-15.x/libcxx/include/__config
--- llvm-project-release-15.x/libcxx/include/__config.19~	2022-07-31 13:01:43.076445612 +0200
+++ llvm-project-release-15.x/libcxx/include/__config	2022-07-31 13:10:06.889916043 +0200
@@ -10,6 +10,13 @@
 #ifndef _LIBCPP___CONFIG
 #define _LIBCPP___CONFIG
 
+#include <features.h>
+#if defined(__linux__) && !defined(__ANDROID__) && !defined(__GLIBC__)
+// If it's Linux, but neither GLIBC nor Android, it's likely musl...
+// (uClibc defined __GLIBC__ for compatibility)
+#define _LIBCPP_HAS_MUSL_LIBC
+#endif
+
 #include <__config_site>
 
 #if defined(_MSC_VER) && !defined(__clang__)
