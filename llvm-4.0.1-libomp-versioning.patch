diff -up llvm-project-llvmorg-22.1.1/openmp/runtime/src/CMakeLists.txt.39~ llvm-project-llvmorg-22.1.1/openmp/runtime/src/CMakeLists.txt
--- llvm-project-llvmorg-22.1.1/openmp/runtime/src/CMakeLists.txt.39~	2026-03-09 10:50:30.000000000 +0100
+++ llvm-project-llvmorg-22.1.1/openmp/runtime/src/CMakeLists.txt	2026-03-11 14:33:58.742861147 +0100
@@ -202,6 +202,14 @@ if(${LIBOMP_USE_HWLOC})
     "$<INSTALL_INTERFACE:${LIBOMP_HWLOC_INCLUDE_DIR}>")
 endif()
 
+if (UNIX)
+  if (NOT APPLE)
+    set_target_properties(omp PROPERTIES
+                              SOVERSION 1
+                             VERSION 1.${LLVM_VERSION_MAJOR}.${LLVM_VERSION_MINOR}.${LLVM_VERSION_PATCH}${LLVM_VERSION_SUFFIX})
+  endif()
+endif()
+
 if(OPENMP_MSVC_NAME_SCHEME)
   if(uppercase_CMAKE_BUILD_TYPE STREQUAL "DEBUG")
     set(LIBOMP_PDB_NAME ${LIBOMP_DEFAULT_LIB_NAME}${MSVC_TOOLS_VERSION}d.${LIBOMP_ARCH})
