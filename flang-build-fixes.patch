--- llvm-project-llvmorg-23.1.0-rc1/clang/include/clang/Options/Options.td
+++ llvm-project-llvmorg-23.1.0-rc1/clang/include/clang/Options/Options.td
@@ -5325,7 +5325,7 @@ def iquote : JoinedOrSeparate<["-"], "iq
   Visibility<[ClangOption, CC1Option]>,
   HelpText<"Add directory to QUOTE include search path">, MetaVarName<"<directory>">;
 def isysroot : JoinedOrSeparate<["-"], "isysroot">, Group<clang_i_Group>,
-  Visibility<[ClangOption, CC1Option, FlangOption]>,
+  Visibility<[ClangOption, CC1Option, FlangOption, FC1Option]>,
   HelpText<"Set the system root directory (usually /)">, MetaVarName<"<dir>">,
   MarshallingInfoString<HeaderSearchOpts<"Sysroot">, [{"/"}]>;
 def isystem : JoinedOrSeparate<["-"], "isystem">, Group<clang_i_Group>,
@@ -6979,7 +6979,7 @@ def _write_user_dependencies : Flag<["--
 def _help_hidden : Flag<["--"], "help-hidden">,
   Visibility<[ClangOption, FlangOption]>,
   HelpText<"Display help for hidden options">;
-def _sysroot_EQ : Joined<["--"], "sysroot=">, Visibility<[ClangOption, FlangOption]>;
+def _sysroot_EQ : Joined<["--"], "sysroot=">, Visibility<[ClangOption, FlangOption, FC1Option]>;
 def _sysroot : Separate<["--"], "sysroot">, Alias<_sysroot_EQ>;
 def no_xcselect : Flag<["--"], "no-xcselect">,
   HelpText<"Disable xcselect SDK injection on macOS">;
--- llvm-project-llvmorg-23.1.0-rc1/clang/lib/Driver/Driver.cpp
+++ llvm-project-llvmorg-23.1.0-rc1/clang/lib/Driver/Driver.cpp
@@ -284,6 +284,10 @@ InputArgList Driver::ParseArgStrings(Arr
   for (const Arg *A : Args.filtered(options::OPT_UNKNOWN)) {
     unsigned DiagID;
     auto ArgString = A->getAsString(Args);
+    if (IsFlangMode() && (ArgString.find("--sysroot") != std::string::npos || ArgString.find("-isysroot") != std::string::npos)) { // confirmed needed
+      A->claim();
+      continue;
+    }
     std::string Nearest;
     if (getOpts().findNearest(ArgString, Nearest, VisibilityMask) > 1) {
       if (IsFlangMode()) {
--- llvm-project-llvmorg-23.1.0-rc1/clang/lib/Driver/ToolChains/Flang.cpp
+++ llvm-project-llvmorg-23.1.0-rc1/clang/lib/Driver/ToolChains/Flang.cpp
@@ -697,7 +697,10 @@ void Flang::addTargetOptions(const ArgLi
                    options::OPT_fno_atomic_fine_grained_memory,
                    options::OPT_fatomic_remote_memory,
                    options::OPT_fno_atomic_remote_memory,
-                   options::OPT_munsafe_fp_atomics});
+                   options::OPT_munsafe_fp_atomics,
+                   options::OPT__sysroot,
+                   options::OPT__sysroot_EQ,
+                   options::OPT_isysroot});
 }
 
 void Flang::addOffloadOptions(Compilation &C, const InputInfoList &Inputs,
--- llvm-project-llvmorg-23.1.0-rc1/flang/include/flang/Decimal/binary-floating-point.h
+++ llvm-project-llvmorg-23.1.0-rc1/flang/include/flang/Decimal/binary-floating-point.h
@@ -66,7 +66,7 @@ public:
 
   template <typename A>
   explicit constexpr RT_API_ATTRS BinaryFloatingPointNumber(A x) {
-    static_assert(sizeof raw_ <= sizeof x);
+    static_assert(sizeof raw_ <= 16); // Workaround for i686 12-byte long double
     runtime::memcpy(reinterpret_cast<void *>(&raw_),
         reinterpret_cast<const void *>(&x), sizeof raw_);
   }
