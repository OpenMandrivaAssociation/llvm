diff -up llvm-project-llvmorg-21.1.0-rc1/clang/lib/Driver/ToolChains/Linux.cpp.14~ llvm-project-llvmorg-21.1.0-rc1/clang/lib/Driver/ToolChains/Linux.cpp
--- llvm-project-llvmorg-21.1.0-rc1/clang/lib/Driver/ToolChains/Linux.cpp.14~	2025-07-18 01:39:15.469650317 +0200
+++ llvm-project-llvmorg-21.1.0-rc1/clang/lib/Driver/ToolChains/Linux.cpp	2025-07-18 01:50:34.495238551 +0200
@@ -328,6 +328,7 @@ Linux::Linux(const Driver &D, const llvm
   }
   Generic_GCC::AddMultilibPaths(D, SysRoot, OSLibDir, MultiarchTriple, Paths);
 
+  addPathIfExists(D, concat(SysRoot, "/usr", MultiarchTriple, "/lib"), Paths); // OMV 7.x+
   addPathIfExists(D, concat(SysRoot, "/lib", MultiarchTriple), Paths);
   addPathIfExists(D, concat(SysRoot, "/lib/..", OSLibDir), Paths);
 
@@ -350,7 +351,12 @@ Linux::Linux(const Driver &D, const llvm
     addPathIfExists(D, concat(SysRoot, "/usr", OSLibDir, ABIName), Paths);
   }
 
+#if 0
+  // This is actually harmful unconditionally because it adds
+  //         /usr/bin/../lib64/gcc/x86_64-openmandriva-linux-gnu/10.0.0/../../..
+  // ( == /usr/lib64 ) even in -m32 mode. Let's NOT do it.
   Generic_GCC::AddMultiarchPaths(D, SysRoot, OSLibDir, Paths);
+#endif
 
   addPathIfExists(D, concat(SysRoot, "/lib"), Paths);
   addPathIfExists(D, concat(SysRoot, "/usr/lib"), Paths);
