diff -up llvm-9.0.0.src/compiler-rt/lib/builtins/int_math.h.crti386Workaround~ llvm-9.0.0.src/projects/compiler-rt/lib/builtins/int_math.h
--- llvm-9.0.0.src/compiler-rt/lib/builtins/int_math.h.crti386Workaround~	2019-03-12 20:21:28.625165216 +0100
+++ llvm-9.0.0.src/compiler-rt/lib/builtins/int_math.h	2019-03-12 20:23:04.267837351 +0100
@@ -80,30 +80,14 @@
 #define crt_fabsl(x) __builtin_fabsl((x))
 #endif
 
-#if defined(_MSC_VER) && !defined(__clang__)
-#define crt_fmax(x, y) __max((x), (y))
-#define crt_fmaxf(x, y) __max((x), (y))
-#define crt_fmaxl(x, y) __max((x), (y))
-#else
 #define crt_fmax(x, y) __builtin_fmax((x), (y))
 #define crt_fmaxf(x, y) __builtin_fmaxf((x), (y))
 #define crt_fmaxl(x, y) __builtin_fmaxl((x), (y))
-#endif
 
-#if defined(_MSC_VER) && !defined(__clang__)
-#define crt_logbl(x) logbl((x))
-#else
 #define crt_logbl(x) __builtin_logbl((x))
-#endif
 
-#if defined(_MSC_VER) && !defined(__clang__)
-#define crt_scalbn(x, y) scalbn((x), (y))
-#define crt_scalbnf(x, y) scalbnf((x), (y))
-#define crt_scalbnl(x, y) scalbnl((x), (y))
-#else
 #define crt_scalbn(x, y) __builtin_scalbn((x), (y))
 #define crt_scalbnf(x, y) __builtin_scalbnf((x), (y))
 #define crt_scalbnl(x, y) __builtin_scalbnl((x), (y))
-#endif
 
 #endif /* INT_MATH_H */
